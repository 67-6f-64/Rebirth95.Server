namespace Rebirth.Common.Types
{
    public enum AffectedAreaType
    {
		// Nexon names now :3
        MobSkill = 0,
        UserSkill = 1,
        Smoke = 2,
        Buff = 3, // this is the cash shop affected area thing
        BlessedMist = 4 // evan recovery aura
    }
}
